# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Claude Code local (personal) settings — team-shared config is committed, this is per-machine
.claude/settings.local.json

# Sidecar del MCP built-in: artefacto de build (se genera en beforeBuildCommand)
src-tauri/binaries/

# Playwright MCP session artifacts (screenshots/snapshots from ui-reviewer runs)
.playwright-mcp/
# Screenshots que a veces caen en la raíz durante reviews de UI
/*.png
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
